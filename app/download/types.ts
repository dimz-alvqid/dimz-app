export interface ContainerData {
  name: string;
  id: string;
  showcaselink: string;
  shortenerlink: string;
  alternativelink?: string;
  hidealternativelink?: boolean;
  targetlink: string;
  redirectlink: string;
}
